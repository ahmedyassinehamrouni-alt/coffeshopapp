/// Set to [true] to bypass Firebase Auth and use hardcoded test credentials.
///
/// ⚠️  NEVER ship a release build with this set to true.
/// All offline users are defined in [OfflineAuthRepository].
const bool kOfflineMode = true;
